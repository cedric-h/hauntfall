let load_map = |map_entries| {
	for entry in map_entries {
		if entry["appearance"] == "Hammer" {
			entry["chaser"] = [
				"target": "Players",
				"distance": 2.5,
				"speed": -0.1,
			];
		}
	}
	return map_entries;
};
